<?xml version="1.0" encoding="utf-8"?>
<!--
  Least privilege: only foreground-window changes, and no access to window
  content (canRetrieveWindowContent=false). SafeGuard sees the package name
  of the app that opened, nothing on screen.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagDefault"
    android:canRetrieveWindowContent="false"
    android:isAccessibilityTool="false"
    android:notificationTimeout="300"
    android:description="@string/app_guard_description"
    android:summary="@string/app_guard_summary" />
